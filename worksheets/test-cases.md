# ตาราง test case ขั้นที่ 4 (แจกผู้เรียน — พิมพ์ใส่กระดาษได้)

ชื่อ ......................................... ชื่องานของฉัน .........................................

## ส่วนที่ 1 — ทดสอบจากกติกา R1–R5

| | ทำอะไร | คาดว่าจะเกิด | เกิดจริง | ✓ / ✗ |
|---|---|---|---|---|
| R1 | ลงทะเบียนด้วยอีเมลที่เคยใช้กับงานนี้แล้ว | ถูกปฏิเสธ พร้อมข้อความบอกเหตุผล | | |
| R1 | ใช้อีเมลเดิม แต่พิมพ์ตัวใหญ่บ้างเล็กบ้าง | ถูกปฏิเสธเหมือนกัน | | |
| R2 | ลงจนครบ capacity แล้วลงเพิ่มอีก 1 คน | ปุ่มหายไป หรือถูกปฏิเสธ | | |
| R2 | ตั้ง capacity = 3 แล้วลงให้ครบ 3 | คนที่ 3 ต้องลงได้ (ไม่ใช่แค่ 2) | | |
| R3 | กด submit ตอนฟอร์มยังว่าง | ขึ้นเตือน ไม่บันทึก | | |
| R3 | กรอกอีเมลที่ไม่มี @ | ขึ้นเตือน ไม่บันทึก | | |
| R3 | กรอกเบอร์ 9 หลัก | ขึ้นเตือน ไม่บันทึก | | |
| R3 | ไม่ติ๊กช่องยินยอม PDPA แล้วกดส่ง | ขึ้นเตือน ไม่บันทึก | | |
| R4 | เปิด `/admin` โดยยังไม่ได้เข้าสู่ระบบ | ไม่เห็นรายชื่อใด ๆ เลย | | |
| R4 | เปิด View Source แล้วค้นหารหัสผ่านของตัวเอง | หาไม่เจอ | | |
| R4 | เปิด `/api/admin/registrations` ตรง ๆ ในแท็บใหม่ | ได้ 401 ไม่มีข้อมูลหลุด | | |
| R4 | ล็อกอินด้วยบัญชีที่**ไม่ได้**อยู่ในตาราง `admins` | เข้าไม่ได้ พร้อมข้อความบอกเหตุผล | | |
| R5 | กดปุ่มส่งรัว ๆ 3 ครั้งติด | ได้ 1 แถว ไม่ใช่ 3 | | |
| R5 | เทียบ ticket_code ของตัวเองกับของเพื่อน 5 คน | ไม่ซ้ำกันสักคน | | |

## ส่วนที่ 2 — ทดสอบความปลอดภัยของฐานข้อมูล

| | ทำอะไร | คาดว่าจะเกิด | เกิดจริง | ✓ / ✗ |
|---|---|---|---|---|
| S1 | เปิดหน้าต่างไม่ระบุตัวตน แล้วยิง REST ของ Supabase ที่ตาราง `registrations` ด้วย anon key | ว่างเปล่า หรือ error เรื่องสิทธิ์ | | |
| S2 | ยิงแบบเดียวกันที่ตาราง `events` | ได้ข้อมูลกิจกรรมกลับมา (ตั้งใจให้เปิด) | | |
| S3 | ค้นคำว่า `sb_secret` และ `service_role` ใน DevTools → Sources | ต้องหาไม่เจอ | | |
| S4 | ยิง REST แบบ POST เพิ่มแถวเข้าตาราง `registrations` ด้วย anon key | ถูกปฏิเสธเรื่องสิทธิ์ | | |
| S5 | เปิด `.env.local` นับจำนวนบรรทัดที่เป็นตัวแปร | ต้องมี 2 บรรทัด และขึ้นต้นด้วย `NEXT_PUBLIC_` ทั้งคู่ | | |

คำสั่งสำหรับ S1 / S2:

```bash
curl "https://<project>.supabase.co/rest/v1/registrations?select=*" -H "apikey: <anon-key>"
curl "https://<project>.supabase.co/rest/v1/events?select=*"        -H "apikey: <anon-key>"
```

คำสั่งสำหรับ S4 (ใส่ `event_id` ของงานตัวเองแทน `...`):

```bash
curl -X POST "https://<project>.supabase.co/rest/v1/registrations" \
     -H "apikey: <anon-key>" -H "Content-Type: application/json" \
     -d '{"event_id":"...","full_name":"ทดสอบ","email":"a@b.co","phone":"0800000000","ticket_code":"X"}'
```

## ส่วนที่ 3 — prompt สำหรับให้ agent เขียน test ให้

```
เขียน test จากไฟล์ requirements.md เท่านั้น ห้ามอ่านโค้ดที่เขียนไว้แล้ว
ครอบคลุมกติกา R1 ถึง R5 ให้ครบทุกข้อ
ถ้าสเปกข้อไหนกำกวมจนเขียน test ไม่ได้ ให้ถามฉัน อย่าเดาเอง
ถ้าต้องติดตั้ง library เพิ่ม ให้บอกฉันก่อน อย่าเพิ่งติดตั้งเอง
```

**กับดักที่ต้องเตือนตรง ๆ**
test ที่ AI เขียนเองจากโค้ดของตัวเอง มักจะผ่านหมด เพราะมันเขียนให้ผ่าน ไม่ได้เขียนเพื่อจับผิด
วิธีกันคือ **ล็อกแหล่งข้อมูล** ให้อ่านจากสเปกอย่างเดียว แล้วเอา test ไปรันกับโค้ดที่แทรกบั๊กไว้
ถ้า test ผ่านทั้งที่บั๊กยังอยู่ แปลว่า test ใช้ไม่ได้ ไม่ใช่โค้ดถูก
