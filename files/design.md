# RSU Event Register — การออกแบบ (ผลลัพธ์ขั้นที่ 2 ออกแบบ)

## 1. ภาพรวมสถาปัตยกรรม

```
           เบราว์เซอร์                        เซิร์ฟเวอร์ (Vercel)                 Supabase
┌───────────────────────────┐        ┌──────────────────────────────┐      ┌──────────────────┐
│  /          Server Comp.  │──────► │  แสดงผลบนเซิร์ฟเวอร์         │─────►│ events           │ อ่านได้สาธารณะ
│  /register  Client Comp.  │──POST─►│  /api/register               │─rpc─►│ create_registra- │ ★ ประตูเดียว
│  /admin     Client Comp.  │──fetch►│  /api/admin/login            │      │ tion()           │    ที่เขียนได้
│                           │        │  /api/admin/registrations    │─JWT─►│ registrations    │ ★ แอดมินที่
│                           │        │  /api/admin/checkin          │      │ (RLS)            │    ล็อกอินเท่านั้น
└───────────────────────────┘        └──────────────────────────────┘      └──────────────────┘
    NEXT_PUBLIC_SUPABASE_*              คุกกี้ httpOnly เก็บ access token       policy + ฟังก์ชัน
      (ทุกคนเห็นได้)                      ของแอดมินที่ล็อกอินแล้ว              คือคนตัดสินสิทธิ์
```

**โปรเจกต์นี้ไม่มีคีย์ลับเลยแม้แต่ตัวเดียว** ตัวแปรที่ใช้มี 2 ตัวและเปิดเผยได้ทั้งคู่
ความปลอดภัยจึงไม่ได้มาจากการซ่อนคีย์ แต่มาจาก 3 ชั้นนี้

| ชั้น | ทำหน้าที่ |
|---|---|
| Row Level Security (`db/rls.sql`) | ใครอ่านแถวไหนได้ — `registrations` เปิดให้เฉพาะแอดมินที่ล็อกอินแล้ว |
| ฟังก์ชัน `security definer` (`db/schema.sql`) | ประตูเดียวที่เขียน `registrations` ได้ และเป็นที่ที่ R1 R2 R5 บังคับจริง |
| Supabase Auth + ตาราง `admins` | รหัสผ่านแอดมินอยู่ในระบบ auth ไม่ได้อยู่ในโค้ดหรือไฟล์ตั้งค่า |

**หลักการเดียวที่ต้องจำ:** ตาราง `registrations` ไม่มี policy ให้ `anon` เลย
เบราว์เซอร์แตะตารางนี้ตรง ๆ ไม่ได้ ทุกอย่างต้องผ่าน route handler ใต้ `src/app/api/`

### ทำไมไม่ให้หน้าแอดมินคุยกับฐานข้อมูลตรง ๆ

วิธีที่ AI มักจะเขียนให้ คือหน้าแอดมินเป็น Client Component แล้วใช้ anon key อ่าน `registrations` ตรง ๆ
มันรันผ่านก็จริง แต่แปลว่า **ต้องเปิด policy ให้ใครก็อ่านตารางผู้ลงทะเบียนได้** ใครก็ยิง REST ของ Supabase
ตรง ๆ แล้วดึงชื่อ อีเมล เบอร์โทรของทั้งห้องออกไปได้ โดยไม่ต้องรู้รหัสผ่านแอดมินเลย
รหัสผ่านที่เช็กใน Client Component ก็อยู่ใน bundle ที่ใครก็เปิดอ่านได้

เราจึงทำสองอย่างพร้อมกัน: ย้ายการอ่านไปฝั่งเซิร์ฟเวอร์ **และ** ปิดตารางด้วย RLS
ต่อให้วันหนึ่งมีคนแก้โค้ดฝั่งเซิร์ฟเวอร์ผิด ฐานข้อมูลก็ยังไม่ยอมคายข้อมูลอยู่ดี — R4 จึงเป็นของจริง ไม่ใช่ผ้าคลุม

### ทำไมไม่ใช้คีย์ลับ (secret key) เหมือนที่ตัวอย่างส่วนใหญ่ทำ

คีย์ลับข้าม RLS ได้ **ทุกตาราง ทุกแถว** ทำให้มันสะดวกมาก และอันตรายมากพอ ๆ กัน

- เผลอ import ในไฟล์ที่มี `"use client"` ครั้งเดียว = คีย์ไปอยู่ในเบราว์เซอร์ของทุกคน
- เผลอ commit ขึ้น git ครั้งเดียว = อยู่ในประวัติตลอดไป ต้องไปเพิกถอนคีย์ทิ้งอย่างเดียว
- และตราบใดที่มันยังใช้ได้ ก็ยังไม่มีอะไรจำกัดว่ามันทำอะไรได้บ้าง

ฟังก์ชัน `security definer` ให้ผลลัพธ์เดียวกันแต่แคบกว่ามาก: มันข้าม RLS ได้เฉพาะงานที่เราเขียนไว้ในตัวมัน
`create_registration()` เพิ่มผู้ลงทะเบียนได้ แต่ลบใครไม่ได้ อ่านรายชื่อไม่ได้ แก้ `capacity` ไม่ได้

## 2. โครงหน้าจอและ flow

### เส้นทางผู้เข้าร่วม

```
/  หน้าแรก                    /register  ฟอร์ม                หน้าสำเร็จ
เห็นรายละเอียด          ──►   กรอกข้อมูล + ติ๊กยินยอม   ──►   แสดงรหัสลงทะเบียน
และที่นั่งคงเหลือ              ระบบตรวจความถูกต้อง              ของตัวเอง
(เต็ม → ปุ่มหาย R2)            (R3 → R1 → R2 → R5)
```

### เส้นทางผู้ดูแล — แยกออกจากกันคนละเส้น

```
/admin  ──►  ใส่อีเมล + รหัสผ่าน  ──►  ตารางผู้ลงทะเบียน
เข้าจาก URL      POST /api/admin/login          ค้นหา · เช็คอิน · ส่งออก CSV
โดยตรง          เซิร์ฟเวอร์ล็อกอินกับ Supabase   ทุก request แนบคุกกี้อัตโนมัติ
                แล้วเก็บ access token
                ไว้ในคุกกี้ httpOnly (R4)
```

การตรวจ 2 ข้อที่ต้องแยกให้ขาด — `requireAdmin()` ใน `src/lib/adminAuth.ts` ทำทั้งคู่

1. **ล็อกอินแล้วหรือยัง** — token ถูกต้องและยังไม่หมดอายุไหม (Supabase Auth ตอบ)
2. **เป็นแอดมินไหม** — `auth.uid()` อยู่ในตาราง `admins` ไหม (ฟังก์ชัน `is_admin()` ตอบ)

ผ่านข้อ 1 ไม่ได้แปลว่าผ่านข้อ 2 ถ้าวันไหนเปิดให้คนทั่วไปสมัครสมาชิก คนที่สมัครจะเป็น `authenticated` ทันที
policy ที่เขียนว่า `to authenticated using (true)` จะกลายเป็น "เปิดให้ทุกคน" โดยที่ไม่มีใครทันสังเกต

หน้าแอดมินมีลิงก์อยู่บนแถบเมนู ("สำหรับเจ้าหน้าที่") และรหัสผ่าน**ไม่โชว์บนหน้าจอ**

> **ทำไมโชว์ลิงก์ได้ ไม่ใช่ช่องโหว่** — การกันคนเข้าอยู่ที่ `requireAdmin()` และ policy ใน `db/rls.sql`
> ไม่ใช่ที่การซ่อน URL การ "ซ่อนทางเข้า" ไม่เคยเป็นการป้องกัน (security through obscurity)
> คนที่ตั้งใจหาเดา `/admin` ได้ใน 3 วินาที ถ้าระบบพังเพราะมีคนรู้ URL แปลว่าด่านจริงไม่เคยมีอยู่

> **บัญชีแอดมินสร้างที่ไหน** — Supabase Dashboard → Authentication → Users → Add user
> แล้วเพิ่ม `user_id` ลงตาราง `admins` (คำสั่งพร้อมใช้อยู่ท้ายไฟล์ `db/seed.sql`)
> ทั้งหมดนี้อยู่นอกโค้ด จึงไม่มีอะไรให้หลุดขึ้น git ได้

## 3. โครงตาราง

แหล่งความจริงคือ `db/schema.sql` ตารางนี้สรุปการตัดสินใจ

### events

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | `uuid` PK | `gen_random_uuid()` |
| `name` | `text` not null | ชื่องานของผู้เรียนแต่ละคน |
| `description` | `text` | |
| `event_date` | `timestamptz` not null | เก็บเป็น timestamptz เพื่อไม่ให้เพี้ยนข้าม timezone |
| `location` | `text` | |
| `capacity` | `int` not null, `> 0` | ใช้กับ R2 |
| `created_at` | `timestamptz` | |

### registrations

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | `uuid` PK | |
| `event_id` | `uuid` FK → `events.id` | `on delete cascade` |
| `full_name` | `text` not null | |
| `email` | `text` not null | เก็บตามที่ผู้ใช้พิมพ์ แต่เทียบซ้ำด้วย `lower()` |
| `phone` | `text` not null | **text ไม่ใช่ number** — `08…` จะกลายเป็น `8…` ทันที |
| `faculty` | `text` | ไม่บังคับ |
| `shirt_size` | `text` | ★ ฟิลด์ส่วนตัว — เปลี่ยนเป็นของตัวเองได้ |
| `ticket_code` | `text` not null | unique (R5) |
| `checked_in` | `boolean` default `false` | |
| `consent_at` | `timestamptz` | เวลาที่ติ๊กยินยอม PDPA |
| `created_at` | `timestamptz` | |

### ทำไมต้อง 2 ตาราง

ถ้ายัดทุกอย่างไว้ตารางเดียว ชื่องานกับสถานที่จะถูกเขียนซ้ำทุกแถวที่มีคนสมัคร
แก้ชื่องานทีเดียวต้องไล่แก้ทุกแถว และถ้าไล่ไม่ครบจะได้ชื่องานสองเวอร์ชันในตารางเดียวกัน

### 4 ข้อที่ต้องตรวจเมื่อ AI เสนอ schema มา

1. **ฟิลด์ครบไหม** — ไล่ทีละหน้าจอ หน้านั้นต้องแสดงอะไร ฟิลด์นั้นมีหรือยัง (ฟิลด์ส่วนตัวของคุณอยู่ในนั้นด้วยไหม)
2. **ชนิดถูกไหม** — เบอร์โทรต้อง `text`, วันเวลาต้อง `timestamptz`, ที่นั่งต้อง `int`
3. **ความสัมพันธ์ถูกไหม** — `registrations.event_id` ต้องชี้กลับไป `events.id` ไม่ใช่เก็บชื่องานซ้ำเป็นข้อความ
4. **กติกาอยู่ในตารางหรือยัง** — R1 มี unique index ไหม, R5 มี unique ไหม, R3 มี `check` ไหม, R2 มี trigger ไหม

## 4. กติกาอยู่ชั้นไหนบ้าง

| กติกา | เบราว์เซอร์ | เซิร์ฟเวอร์ | ฐานข้อมูล |
|---|---|---|---|
| R1 อีเมลซ้ำ | – | ส่งต่อให้ฟังก์ชันใน DB ตัดสิน | `create_registration()` + `unique (event_id, lower(email))` |
| R2 ที่นั่งเต็ม | ซ่อนปุ่ม | อ่านที่นั่งคงเหลือจาก `seats_taken()` | `create_registration()` ล็อกแถวด้วย `for update` + trigger |
| R3 ฟอร์ม | เตือนทันที | `validateRegistration()` | `check (phone ~ '^[0-9]{10}$')` + ตรวจซ้ำในฟังก์ชัน |
| R4 แอดมิน | ซ่อน UI | `requireAdmin()` ตรวจคุกกี้ทุก request | policy `is_admin()` + ไม่มี policy ให้ `anon` |
| R5 รหัสไม่ซ้ำ | – | สุ่มใหม่ retry 5 ครั้ง | `unique (ticket_code)` |

**ชั้นเบราว์เซอร์คือความสะดวก ชั้นฐานข้อมูลคือความจริง** — ถ้ากติกาไหนมีแค่ในเบราว์เซอร์ ถือว่ายังไม่มี

### ทำไม R2 ต้องจบที่ฐานข้อมูล ไม่ใช่ที่เซิร์ฟเวอร์

เพราะ "นับแล้วค่อย insert" เป็นสองคำสั่งที่แยกกัน ถ้ามีคนกดพร้อมกัน 5 คนตอนเหลือที่นั่งสุดท้าย
ทั้ง 5 คนจะนับได้ 29 เหมือนกันหมด แล้ว insert ผ่านทั้ง 5 คน — ได้ 34 คนในงานที่รับ 30

`create_registration()` แก้ด้วยการทำทั้งหมดนี้ใน **คำสั่งเดียว** และเริ่มด้วย `select … for update`
ซึ่งล็อกแถว `events` ไว้ คำสั่งของ 5 คนนั้นจึงเข้าคิวทีละคน คนที่ 30 ผ่าน คนที่ 31 ได้ `EVENT_FULL`
trigger `enforce_capacity()` ยังอยู่เป็นตาข่ายชั้นสุดท้าย เผื่อวันหนึ่งมีใครเพิ่มแถวด้วยวิธีอื่น

## 5. ข้อความ error ที่ผู้ใช้จะเห็น (ภาษาไทยทั้งหมด)

| สถานการณ์ | รหัส | ข้อความ |
|---|---|---|
| เว้นช่องบังคับ | `MISSING_FIELD` | กรุณากรอกข้อมูลที่จำเป็นให้ครบถ้วน |
| อีเมลผิดรูปแบบ | `INVALID_EMAIL` | รูปแบบอีเมลไม่ถูกต้อง |
| เบอร์ไม่ใช่ 10 หลัก | `INVALID_PHONE` | เบอร์โทรศัพท์ต้องเป็นตัวเลข 10 หลัก |
| ยังไม่ติ๊กยินยอม | `CONSENT_REQUIRED` | กรุณายอมรับเงื่อนไขการเก็บข้อมูลส่วนบุคคลก่อนลงทะเบียน |
| อีเมลซ้ำ (R1) | `DUPLICATE_EMAIL` | อีเมลนี้ลงทะเบียนกิจกรรมนี้ไปแล้ว |
| ที่นั่งเต็ม (R2) | `EVENT_FULL` | ขออภัย ที่นั่งเต็มแล้ว |
| ออกรหัสไม่สำเร็จ (R5) | `TICKET_CODE_COLLISION` | ระบบออกรหัสลงทะเบียนไม่สำเร็จ กรุณาลองใหม่อีกครั้ง |
| ไม่ได้เข้าสู่ระบบแอดมิน | `UNAUTHORIZED` | กรุณาเข้าสู่ระบบก่อน |

`DUPLICATE_EMAIL` กับ `TICKET_CODE_COLLISION` **ต้องเป็นคนละข้อความ** ทั้งที่ทั้งคู่มาจาก error `23505` ของ Postgres
การแยกสองอันนี้คือจุดที่ทุกคนพลาด และเป็นสิ่งที่ AC-5.4 ตรวจ

## 6. ตัวแปร environment

ทั้งโปรเจกต์ใช้แค่ 2 ตัว และทั้งคู่เปิดเผยได้

| ชื่อ | ฝั่ง | ใช้ทำอะไร |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | public | ที่อยู่ฐานข้อมูล |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | public | คีย์สาธารณะ — ทำได้เท่าที่ RLS และ grant อนุญาต |

ตัวที่ขึ้นต้นด้วย `NEXT_PUBLIC_` จะถูกฝังลงไฟล์ JavaScript ที่ส่งให้เบราว์เซอร์ **ทุกคนอ่านได้**
ที่นี่ไม่เป็นไร เพราะทั้งสองค่านี้ไม่ได้ให้สิทธิ์อะไรเกินกว่าที่ `db/rls.sql` เขียนไว้

**แล้วของที่ต้องลับอยู่ไหน**

| สิ่งที่ต้องปกป้อง | เก็บไว้ที่ไหน | ทำไมปลอดภัยกว่าเดิม |
|---|---|---|
| รหัสผ่านแอดมิน | Supabase Auth (Dashboard → Authentication → Users) | ไม่อยู่ในโค้ด ไม่อยู่ในไฟล์ `.env` จึงไม่มีอะไรให้ commit พลาด |
| สิทธิ์อ่าน `registrations` | policy ใน `db/rls.sql` | ย้ายจาก "ใครถือคีย์" มาเป็น "ใครคือใคร" — เพิกถอนทีละคนได้ |
| session ของแอดมิน | คุกกี้ httpOnly เก็บ access token ของ Supabase | หมดอายุเอง เพิกถอนได้ และ JavaScript ในหน้าเว็บอ่านไม่ได้ |

> ★ ถ้าวันไหนเห็นตัวแปรที่ไม่ได้ขึ้นต้นด้วย `NEXT_PUBLIC_` โผล่เข้ามาในโปรเจกต์นี้
> แปลว่ามีคน (หรือ AI) กำลังพาเรากลับไปพึ่งการซ่อนคีย์อีกครั้ง — ให้ถามก่อนเสมอว่าทำไมถึงจำเป็น

## 7. ลำดับการรัน SQL

```
db/schema.sql   →   db/rls.sql   →   db/seed.sql
 ตาราง/index/trigger   สิทธิ์เข้าถึง     กิจกรรมตัวอย่าง 1 แถว
```

รันสลับลำดับไม่ได้ — `rls.sql` สั่ง `grant execute` ให้ฟังก์ชันที่ `schema.sql` เป็นคนสร้าง
และ `seed.sql` ต้องมีตาราง `admins` อยู่ก่อนจึงจะเพิ่มผู้ดูแลได้

หลังรันครบ 3 ไฟล์ อย่าลืมอีก 2 ขั้นที่ทำในหน้าเว็บของ Supabase

1. Authentication → Users → **Add user** สร้างบัญชีแอดมิน (ติ๊ก Auto Confirm User ด้วย)
2. กลับมารันท่อนท้ายของ `seed.sql` เพื่อเพิ่ม `user_id` นั้นลงตาราง `admins`
